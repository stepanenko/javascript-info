
// input
const cities = ["paris", "sirap", "rapsi", "london", "onlond", "oslo", "ools", "lviv"];

// output
// [['paris', 'sirap', 'rapsi'], ['london', 'onlond'], ['oslo', 'ools'], ['lviv']]

function groupCities(arr) {
  
}

let result = [];
for (let i = 0; i < cities.length; i++) {
  let group = [cities[i]];
  for (let k = 0; k < cities[i].length; k++) {
    if (cities[])
    cities[i]

  }
}